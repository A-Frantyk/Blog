(function () {
    'use strict'

    function SessionStorage(fkey, fvalue) {
        var key;
        var value;

        if (key !== null && value !== null) {
            key = fkey;
            value = fvalue;
        }
    };
})();